# Windows setup

Install 64-bit Python 3.11, ensure the `py` launcher is available, and follow
`LOCAL_EXECUTION.md`. Quote every path containing spaces. CPU mode is the
default. For CUDA, install the PyTorch wheel matching the locally installed
NVIDIA driver before installing the remaining project extras.
